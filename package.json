{
  "name": "elyra",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "format": "pnpm --filter frontend format && cd backend && mix format",
    "format:check": "pnpm --filter frontend format:check && cd backend && mix format --check-formatted"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "frontend/**/*.{js,ts,svelte,css,html,json,md}": "prettier --write --config frontend/.prettierrc",
    "backend/**/*.{ex,exs,heex}": "mix format --dot-formatter backend/.formatter.exs"
  }
}
